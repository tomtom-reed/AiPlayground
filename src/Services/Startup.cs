namespace Services;

public class Startup
{
    // actually no, this is a library so it doesn't need a startup.
    // it is what gets injected, not what does the injection.
    // So it needs a separate serviceHelper class for the tests. I suppose everything does. 
}
